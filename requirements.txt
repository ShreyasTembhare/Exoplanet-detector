# TESS exoplanet pipeline (Phase 1-4)
lightkurve>=2.0.0
astropy>=5.0.0
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# GPU BLS (optional; NumPy fallback if not installed)
jax>=0.4.0
jaxlib>=0.4.0

# ResNet-1D classifier
torch>=1.12.0

# Optional: labels / centroid vetting
astroquery>=0.4.0

# Utilities
tqdm>=4.64.0
matplotlib>=3.5.0

# Metrics (optional but recommended for training)
scikit-learn>=1.0.0
